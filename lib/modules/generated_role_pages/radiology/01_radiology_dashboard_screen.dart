import 'package:flutter/material.dart';

class RadiologyDashboardScreen extends StatelessWidget {
  const RadiologyDashboardScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Radiology Dashboard')),
      body: const Center(
        child: Text('Radiology Dashboard - Coming Soon'),
      ),
    );
  }
}
