import 'package:flutter/material.dart';

class ClinicAdminDashboardScreen extends StatelessWidget {
  const ClinicAdminDashboardScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Clinic Admin Dashboard')),
      body: const Center(
        child: Text('Clinic Admin Dashboard - Coming Soon'),
      ),
    );
  }
}
