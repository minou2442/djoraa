import 'package:flutter/material.dart';

class LabDashboardScreen extends StatelessWidget {
  const LabDashboardScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Laboratory Dashboard')),
      body: const Center(
        child: Text('Laboratory Dashboard - Coming Soon'),
      ),
    );
  }
}
